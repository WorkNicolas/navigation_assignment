import React from 'react';
import { Text } from 'react-native';

export default function BackButton(){
    return(
        <Text style={{fontSize: 42, color: 'limegreen', left: -150, top: -100}}>⇚</Text>
    )
}